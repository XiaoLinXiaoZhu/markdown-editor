### Added

- `callout` plugin — replaces Obsidian's block-widget callout rendering with line-decoration based implementation: inline icon + title on first line, colored background, rounded corners, stable line heights, visible line numbers, and no layout jumps when entering/exiting edit mode. Uses Obsidian's native icon registry (`setIcon`) for pixel-perfect icon matching.
